This file will keep track of all files we added to either `raw_data` or `app_data`.
This is so that we can manually trace what we have changed so that
when it inevitably comes time to update the payload, we have a record
of what we expect to have changed.

- added `raw_data/ppi`
    - `gene2acc/UP000059680_39947.gene2acc`
    This contains the mapping from UniProt accession to MSU Accession. downloaded from [here](https://ftp.uniprot.org/pub/databases/uniprot/current_release/knowledgebase/reference_proteomes/Eukaryota/UP000059680/).
    - `STRING-Physical/all_proteins.txt` contains list of all proteins inside `STRING-Physical`. Generated by provided commands from Paulo Manlapaz.
    - `STRING-Physical/MSU/all-genes.txt` contains corresponding genes to proteins found in `../all_proteins.txt`Generated by provided commands from Paulo Manlapaz.
- added `app_data/`
    - in `networks/` added `STRING-Physical.txt` and `STRING.txt`.
    - in `gene_id_mapping/` added `msu_mapping/uniprot_to_msu.pickle`. generated from **script(?)**.
    